Thanks @liamfallon! I've updated `internal/kptops/functions.go` to use `:latest` as you suggested.

I'll run the E2E tests locally to update the expected output files. Since I'm on Windows, I'll need to set up a proper environment to run the tests with `KPT_E2E_UPDATE_EXPECTED=true`.

Will push the updated expected files once the tests pass locally.
